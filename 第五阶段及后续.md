
---


## 5. 
## 6. 第五阶段：系统集成与持续优化

### 6.1 模块集成与测试

#### 6.1.1 端到端流程验证：数据→特征→模型→信号→回测→可视化

| 测试层级 | 测试内容 | 验证方法 |
|:---|:---|:---|
| **单元测试** | 各模块函数、类的正确性 | `pytest`，覆盖率>80%，Mock外部依赖 |
| **集成测试** | 模块间接口兼容性 | 数据流完整遍历，边界条件测试 |
| **端到端测试** | 完整业务流程 | 预设场景自动化验证，人工抽查 |
| **回归测试** | 代码变更后原有功能 | CI/CD流水线自动执行 |

**关键测试场景**：数据中断恢复、模型预测异常（NaN/Inf）、极端行情（连续涨跌停）、大订单流动性不足、并发回测资源竞争。

#### 6.1.2 异常处理与监控：数据中断、模型失效、交易异常

| 异常类型 | 检测机制 | 响应策略 | 通知方式 |
|:---|:---|:---|:---|
| **数据中断** | 定时任务超时，最新数据日期滞后 | 重试3次，切换备用源，标记数据缺失 | 邮件+钉钉/企业微信 |
| **模型失效** | 滚动IC连续5日<0或IC_IR<0.3 | 触发再训练，降级到备用模型，暂停交易 | 邮件+Dashboard告警 |
| **交易异常** | 订单被拒绝、价格超出涨跌停、成交延迟 | 记录日志，人工复核，自动撤单重试 | 实时推送+日志归档 |
| **系统性能** | 响应时间>5s，内存使用率>80% | 自动扩容，清理缓存，降级服务 | 监控大盘告警 |

#### 6.1.3 性能优化：数据库查询优化、模型推理加速、并行回测

| 优化方向 | 技术手段 | 预期效果 |
|:---|:---|:---|
| **数据库查询** | 索引优化、查询重写、物化视图、读写分离 | 复杂查询从秒级到毫秒级 |
| **模型推理** | ONNX Runtime/TensorRT量化、批处理、模型蒸馏 | 延迟降低5-10倍，吞吐量提升 |
| **并行回测** | `multiprocessing`多进程、`ray`分布式、参数服务器 | 多策略扫描从小时级到分钟级 |
| **缓存策略** | Redis热点数据、CDN静态资源、浏览器缓存 | 重复请求减少80% |

### 6.2 实盘交易接口预留

#### 6.2.1 模拟交易层：与回测引擎统一接口

**抽象设计原则**：回测、模拟交易、实盘交易共享统一接口，策略代码无需修改即可切换环境。

| 环境 | 数据Feed | 订单执行 | 持仓更新 | 适用场景 |
|:---|:---|:---|:---|:---|
| **回测** | MySQL历史数据 | `Backtrader` broker模拟 | 虚拟计算 | 策略研发，历史验证 |
| **模拟交易** | 实时行情API | 券商仿真系统 | 仿真账户同步 | 策略上线前验证 |
| **实盘交易** | 实时行情API | 券商/期货柜台API | 实际账户同步 | 真实资金运作 |

**统一接口设计**（`execution/base_executor.py`）：
```python
class BaseExecutor(ABC):
    @abstractmethod
    def get_data(self, symbol: str, start: datetime, end: datetime) -> pd.DataFrame: ...
    @abstractmethod
    def place_order(self, order: Order) -> OrderResult: ...
    @abstractmethod
    def get_position(self, symbol: str) -> Position: ...
    @abstractmethod
    def get_account(self) -> AccountInfo: ...
```

#### 6.2.2 券商API适配：QMT、Ptrade等主流接口预留设计

| 平台 | 特点 | 接口类型 | 适配策略 |
|:---|:---|:---|:---|
| **迅投QMT** | 本地执行，Python策略，延迟低 | Python API + C++内核 | 直接继承`BaseExecutor`，本地调用 |
| **恒生Ptrade** | 云端执行，Web管理，稳定性高 | REST API + SDK | HTTP客户端封装，异步回调处理 |
| **聚宽JoinQuant** | 在线平台，数据丰富，社区活跃 | 在线IDE + API | 导出策略代码，适配层转换 |
| **自研柜台** | 完全定制，性能极致 | FIX/二进制协议 | 底层网络库，协议解析 |

**预留设计**：`executors/`目录下`qmt_executor.py`、`ptrade_executor.py`等，统一实现`BaseExecutor`接口，配置文件中`EXECUTOR_TYPE`切换。

### 6.3 策略迭代与模型更新

#### 6.3.1 模型再训练触发：性能衰减检测、定期更新

| 触发条件 | 检测方法 | 再训练流程 |
|:---|:---|:---|
| **性能衰减** | 滚动IC 20日均值 < 历史均值50%，或IC_IR < 0.5 | 自动触发，使用最新数据，A/B测试验证 |
| **定期更新** | 每月/每季度固定时间 | 全量重训练，超参数优化，版本归档 |
| **数据积累** | 新数据量 > 原训练集20% | 增量更新或全量重训练评估 |
| **市场结构变化** | 波动率 regime 切换检测 | 自适应模型，或分 regime 训练 specialist |

#### 6.3.2 新策略快速接入：标准化接口、AB测试框架

| 环节 | 标准化要求 | 工具支持 |
|:---|:---|:---|
| **策略开发** | 继承`BaseStrategy`，实现`generate_signal()` | 代码模板，IDE插件，类型检查 |
| **单元测试** | 覆盖率>80%，边界条件测试 | `pytest`，`hypothesis`属性测试 |
| **回测验证** | 至少3年数据，多市场状态覆盖 | 自动化回测流水线 |
| **AB测试** | 与基准策略并行，统计显著性检验 | 分层流量，假设检验， early stopping |
| **上线部署** | 灰度发布，监控告警，快速回滚 | 容器化，Kubernetes， feature flag |

---

## 7. 附录：提示词使用规范与最佳实践

### 7.1 提示词结构模板

#### 7.1.1 上下文设定：系统角色、技术栈、约束条件

| 要素 | 说明 | 示例 |
|:---|:---|:---|
| **系统角色** | 明确Claude扮演的专业身份 | "你是一位量化交易系统架构师" |
| **技术栈** | 指定语言、框架、库版本 | "Python 3.10+, Backtrader 1.9.76, PyTorch 2.0" |
| **约束条件** | 功能边界、性能要求、合规限制 | "日频数据，T+1制度，无未来信息泄露" |
| **输出格式** | 代码、文档、表格、JSON等 | "Python代码，类型注解，PEP 8规范" |

#### 7.1.2 任务描述：具体功能、输入输出、接口规范

| 要素 | 说明 | 示例 |
|:---|:---|:---|
| **功能目标** | 清晰描述要实现什么 | "实现基于LSTM的价格预测模型" |
| **输入规格** | 数据格式、字段、来源 | "OHLCV DataFrame，30维特征矩阵" |
| **输出规格** | 返回值、文件、数据库记录 | "预测收益率，模型文件，训练日志" |
| **接口定义** | 函数签名、类方法、参数类型 | `def predict(features: np.ndarray) -> float` |
| **依赖关系** | 前置模块、数据准备、环境配置 | "依赖features/engineering.py的输出" |

#### 7.1.3 质量要求：代码规范、测试覆盖、文档注释

| 类别 | 具体要求 | 验证方式 |
|:---|:---|:---|
| **代码规范** | PEP 8，类型注解，命名约定 | `black`, `mypy`, `pylint` |
| **测试覆盖** | 单元测试>80%，边界条件，异常路径 | `pytest`, `pytest-cov` |
| **文档注释** | docstring（Google/NumPy风格），行内注释关键逻辑 | `pydocstyle`, 人工review |
| **性能基准** | 响应时间，内存占用，吞吐量 | 基准测试，profiling |

#### 7.1.4 迭代指令：修改方向、保留内容、废弃内容

| 场景 | 指令格式 | 示例 |
|:---|:---|:---|
| **功能扩展** | "在现有基础上增加X，保持Y不变" | "增加移动止盈功能，保留固定止盈逻辑" |
| **问题修复** | "修复X问题，原因是Y，验证Z" | "修复ATR计算中的未来信息泄露，使用shift(1)" |
| **性能优化** | "优化X，目标从A提升到B，方法不限" | "优化数据库查询，目标从2s降到200ms" |
| **代码重构** | "重构X模块，接口保持不变，内部结构优化" | "重构RiskManager，提取条件判断为策略模式" |
| **废弃删除** | "删除X功能，原因是Y，替代方案Z" | "删除固定滑点模型，统一使用波动率相关滑点" |

### 7.2 多轮对话管理策略

#### 7.2.1 版本控制：代码变更追踪、回滚机制

| 实践 | 工具/方法 | 目的 |
|:---|:---|:---|
| **即时保存** | 每轮对话结束复制代码到文件 | 防止上下文丢失 |
| **Git提交** | 功能完成即commit，清晰message | 历史追溯，协作同步 |
| **分支管理** | feature分支隔离实验 | 稳定main，灵活开发 |
| **标签归档** | 里程碑版本打tag | 重要节点快速恢复 |
| **变更日志** | CHANGELOG.md记录重大变更 | 版本发布，用户沟通 |

#### 7.2.2 知识沉淀：设计决策记录、技术债务管理

| 文档类型 | 内容 | 维护频率 |
|:---|:---|:---|
| **ADR（架构决策记录）** | 重大设计选择的原因、权衡、后果 | 每次重大决策 |
| **README** | 项目概述、快速开始、核心概念 | 持续更新 |
| **API文档** | 模块接口、参数、返回值、示例 | 代码变更时同步 |
| **技术债务清单** | 已知问题、临时方案、改进计划 | 定期review |
| **复盘笔记** | 迭代经验、踩坑记录、最佳实践 | 每个里程碑 |

---

**本路线图完整覆盖了从基础设施搭建到策略研发、回测验证、可视化展示、持续优化的全流程，每个阶段均提供了可直接使用的Claude提示词模板。建议按阶段推进，每个阶段完成后进行集成测试，确保系统稳健演进。**

