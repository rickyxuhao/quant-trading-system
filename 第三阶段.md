## 4. 第三阶段：回测引擎与风险管理系统

### 4.1 回测框架选型与定制

#### 4.1.1 框架对比：Backtrader、Zipline、VectorBT、自研引擎

| 框架 | 架构类型 | 核心优势 | 主要局限 | 推荐场景 |
|:---|:---|:---|:---|:---|
| **Backtrader** | 事件驱动 | 功能全面，灵活扩展，社区活跃，实盘适配成熟 | 纯Python速度较慢，多资产组合复杂 | **中低频策略，复杂逻辑，机器学习集成** |
| **Zipline** | 事件驱动 | Quantopian遗产，Pipeline API强大，PyFolio集成 | 维护放缓，本地化不足，主要支持美股 | 美股研究，因子分析，学术研究 |
| **VectorBT** | 向量化 | NumPy加速极快，参数优化强大，内置指标丰富 | 事件驱动弱，复杂策略表达困难，学习曲线陡 | 大规模参数扫描，快速原型，组合优化 |
| **vn.py** | 事件驱动 | 国内首选，完整实盘支持，GUI界面，社区活跃 | 框架较重，学习成本高，需理解事件引擎 | 国内实盘，完整系统，机构级应用 |
| **自研引擎** | 定制化 | 完全可控，深度优化，特殊需求适配 | 开发成本高，测试负担重，生态孤立 | 高频交易，特殊撮合规则，性能极致 |

**综合推荐**：**Backtrader作为主框架**，VectorBT作为参数优化补充。Backtrader的事件驱动架构天然适配日频交易策略，丰富的内置指标和分析器减少重复开发，活跃的社区提供充足的学习资源与问题解决方案。

#### 4.1.2 Backtrader深度集成：DataFeed、Strategy、Analyzer、Observer

**核心组件定制实现**：

| 组件 | 定制内容 | 技术实现 |
|:---|:---|:---|
| **MySQLDataFeed** | 从数据库读取日频数据 | 继承`bt.feeds.PandasData`，重写`_load()`方法，支持`fromdate`/`todate`参数 |
| **MLStrategy基类** | 统一策略接口，支持模型注入 | 抽象基类定义`generate_signal()`纯虚方法，子类实现具体逻辑 |
| **RiskManager混入** | 止盈止损、仓位控制 | Mixin类提供`check_stop_loss()`、`check_take_profit()`、`calculate_position_size()`方法 |
| **CustomAnalyzer** | 扩展绩效指标 | 继承`bt.Analyzer`，实现`CalmarRatio`、`SortinoRatio`、`TradeStats`等 |
| **MLObserver** | 记录模型预测值 | 继承`bt.Observer`，绘制预测-实际对比曲线 |

#### 4.1.3 多策略结果聚合：统一绩效指标、策略对比、结果持久化

**统一绩效指标标准**：

| 指标类别 | 具体指标 | 计算方式 |
|:---|:---|:---|
| **收益类** | 总收益率、年化收益率、月度收益率 | $(期末/期初 - 1)$，$(1+总收益)^{(252/天数)}-1$ |
| **风险类** | 年化波动率、最大回撤、回撤持续时间 | 收益率标准差×√252，$(峰值-谷值)/峰值$，峰值到恢复的天数 |
| **风险调整收益** | 夏普比率、索提诺比率、Calmar比率 | $(年化收益-无风险利率)/年化波动率$，$(年化收益-无风险利率)/下行标准差$，$年化收益/最大回撤$ |
| **交易统计** | 胜率、盈亏比、平均持仓天数、最大连续盈亏 | 盈利次数/总次数，平均盈利/平均亏损，持仓天数均值，连续盈亏序列 |
| **系统质量** | 盈利因子、期望值、SQN（Van Tharp） | 总盈利/总亏损，胜率×平均盈利 - 败率×平均亏损，$(胜率×盈亏比 - 败率)/标准差$ |

**多策略对比功能**：并排展示关键指标表格、收益曲线叠加图、雷达图多维度可视化、策略相关性矩阵、统计显著性检验（夏普比率差异的t检验）。

**Claude提示词（阶段4.1）：**

> "请为我基于**Backtrader**定制一个完整的回测引擎：1) **MySQLDataFeed**（`data/backtrader_feed.py`）：继承`bt.feeds.PandasData`，从`daily_kline`表读取数据，支持`symbol`、`fromdate`、`todate`、`adj_type`（前复权/后复权/不复权）参数，实现`_load()`逐条yield数据，处理停牌日（跳过或前向填充可配置）；2) **BaseStrategy基类**（`backtest/base_strategy.py`）：抽象基类封装通用逻辑——`__init__()`初始化参数和指标，`next()`调用`generate_signal()`获取信号并执行订单，`notify_order()`跟踪成交更新持仓，`notify_trade()`记录交易盈亏；子类只需实现`generate_signal()`返回信号字典`{'action': 'BUY'/'SELL'/'HOLD', 'size': float, 'reason': str}`；3) **MLStrategy子类**（`backtest/ml_strategy.py`）：接收训练好的模型，在`generate_signal()`中计算特征、调用模型预测、转化为交易信号，支持预测阈值、置信度过滤、信号平滑（如连续3日同向才确认）；4) **成本模拟**：`setcommission`配置股票印花税0.1%（仅卖出）、佣金0.025%双向、最低5元；`set_slippage`支持固定滑点（0.01元）、百分比滑点（0.05%）、波动率相关滑点（0.1×ATR）；5) **自定义Analyzer**（`backtest/analyzers.py`）：`CalmarRatio`、`SortinoRatio`、`TradeDetailAnalyzer`（记录每笔交易完整信息）、`ModelPredictionAnalyzer`（记录模型预测值与实际值）；6) **多策略并行**：`run_multiple_strategies()`函数，接收策略类列表和参数网格，使用`multiprocessing`并行回测，返回统一格式结果DataFrame，写入`backtest_results`表；7) **完整示例**：双均线策略 vs LSTM预测策略，沪深300指数2019-2024回测，展示绩效对比、交易明细、模型诊断。"

### 4.2 止盈止损与仓位管理系统

#### 4.2.1 止盈机制：固定比例止盈、移动止盈、目标价止盈

| 机制 | 触发条件 | 特点 | 适用策略 |
|:---|:---|:---|:---|
| **固定比例止盈** | 持仓收益 ≥ 预设比例（如10%） | 简单直接，纪律性强，可能过早退出趋势 | 均值回归，目标收益明确 |
| **移动止盈（Trailing Stop）** | 价格从最高点回撤 ≥ 比例（如5%） | 动态保护利润，给予趋势延续空间 | 趋势跟踪，动量策略 |
| **目标价止盈** | 价格达到模型预测目标或技术位 | 基于预测或分析，灵活但主观 | 预测模型，技术分析 |
| **分级止盈** | 多档位递减平仓（5%平30%，10%平50%...） | 锁定部分利润，保留上行敞口 | 平衡确定性与弹性 |

#### 4.2.2 止损机制：固定比例止损、波动率止损、时间止损

| 机制 | 触发条件 | 特点 | 风险控制重点 |
|:---|:---|:---|:---|
| **固定比例止损** | 持仓亏损 ≥ 预设比例（如5%） | 简单，控制单笔最大损失 | 灾难性亏损防范 |
| **波动率止损（ATR Stop）** | 亏损 ≥ N×ATR（如2×14日ATR） | 自适应市场波动，噪音过滤 | 正常波动 vs 异常下跌 |
| **时间止损** | 持仓超过最大期限（如20日） | 机会成本管理，防止资金沉淀 | 策略时效性，资金效率 |
| **技术止损** | 价格跌破关键支撑位/均线 | 结合市场结构，主观判断 | 趋势反转确认 |

#### 4.2.3 复合条件：多条件触发、条件优先级、部分平仓

**复合条件设计原则**：多条件"或"关系触发（任一满足即行动），但需定义**优先级**避免冲突。

| 优先级 | 条件类型 | 典型设置 | 处理逻辑 |
|:---|:---|:---|:---|
| 1（最高） | 强制止损 | 亏损8%或ATR×3 | 立即全仓平仓，保护本金 |
| 2 | 移动止盈 | 最高点回撤5% | 动态跟踪，利润保护 |
| 3 | 固定止盈 | 盈利10% | 目标达成，逐步减仓 |
| 4 | 时间止损 | 持仓超25日 | 资金释放，机会成本 |
| 5（最低） | 信号反转 | 模型预测方向改变 | 策略逻辑，重新配置 |

**部分平仓实现**：维护多笔子持仓，每笔独立记录成本、数量、止盈止损位。例如：初始建仓1000股，盈利5%平仓300股（30%），剩余700股继续跟踪；盈利10%再平仓350股（剩余50%），最后350股让利润奔跑或触发移动止盈。

#### 4.2.4 仓位动态调整：Kelly公式、风险平价、最大回撤控制

| 方法 | 核心公式/逻辑 | 参数估计 | 实际应用调整 |
|:---|:---|:---|:---|
| **Kelly公式** | $f^* = (bp - q) / b$ | 历史胜率p，平均盈亏比b | 半Kelly或四分之一Kelly，降低波动 |
| **风险平价** | 各资产/策略风险贡献相等 | 协方差矩阵估计，迭代优化 | 正则化处理，防止极端权重 |
| **波动率目标** | 仓位 ∝ 目标波动率 / 预测波动率 | 滚动波动率估计 | 上限约束，防止过度杠杆 |
| **最大回撤控制** | 回撤接近阈值时降仓，修复后恢复 | 历史最大回撤，当前回撤 | 渐进调整，避免频繁切换 |

**Claude提示词（阶段4.2）：**

> "请为我实现一个**灵活的止盈止损模块RiskManager**（`risk/risk_manager.py`），完整支持以下功能：1) **条件配置**：通过`RiskConfig` dataclass或YAML文件配置，支持固定比例（`profit_pct=0.10`, `loss_pct=0.05`）、移动止盈（`trailing_pct=0.05`）、ATR止损（`atr_multiplier=2.0`, `atr_period=14`）、时间止损（`max_holding_days=20`）、目标收益（`target_profit_pct=0.12`）；2) **复合条件与优先级**：多条件"或"触发，优先级强制止损 > 移动止盈 > 固定止盈 > 时间止损 > 信号反转，冲突时高优先级优先执行；3) **部分平仓**：分级止盈配置`partial_exits=[(0.05, 0.3), (0.10, 0.5), (0.15, 1.0)]`，盈利5%平30%，10%平50%（累计80%），15%平剩余，精确跟踪每笔子持仓的成本和盈亏；4) **Backtrader集成**：作为Strategy的组成成员，在`next()`中调用`self.risk_manager.check_exits()`，返回`exit_signal`（None/'FULL'/'PARTIAL'）、`exit_size`（平仓数量）、`exit_reason`（触发原因），通过`self.close(size=exit_size)`或`self.sell(size=exit_size)`执行；5) **详细记录**：`notify_order()`中记录`exit_reason`到`trade.exit_reason`，`trade.size`和`trade.price`更新子持仓状态，支持后续归因分析；6) **动态调整**：运行时可通过`update_config()`调整参数，如市场波动加剧时提高ATR倍数，策略表现恶化时收紧止损；7) **完整测试**：单元测试覆盖各种条件组合、优先级冲突、部分平仓计算，示例策略演示不同市场情景下的触发逻辑。"

### 4.3 交易成本精细化建模

#### 4.3.1 手续费结构：印花税、佣金、过户费（分资产类别）

| 资产类别 | 印花税 | 佣金 | 过户费 | 其他费用 | 综合成本估算 |
|:---|:---|:---|:---|:---|:---|
| **A股（普通）** | 0.10%（卖出） | 0.025%双向，最低5元 | 0.001%双向（沪市） | 无 | ~0.16%（双向） |
| **A股（创业板/科创板）** | 0.10%（卖出） | 0.025%双向，最低5元 | 0.001%双向（沪市） | 无 | ~0.16%（双向） |
| **ETF** | 无 | 0.025%双向，最低5元 | 无 | 无 | ~0.05%（双向） |
| **场内基金（LOF）** | 无 | 0.025%双向，最低5元 | 无 | 无 | ~0.05%（双向） |
| **场外基金** | 无 | 申购费0.1%-1.5%（打一折后0.015%），赎回费0-0.5% | 无 | 管理费、托管费年化扣除 | ~0.2%-2%（单次申赎） |
| **期货** | 无 | 按合约固定值或比例（如IF 0.23%%） | 无 | 保证金占用利息 | ~0.05%（单次，不含资金成本） |

#### 4.3.2 滑点模型：固定滑点、百分比滑点、波动率相关滑点

| 模型 | 公式 | 参数 | 适用场景 |
|:---|:---|:---|:---|
| **固定滑点** | `slippage = fixed_amount`（如0.01元） | `fixed_pips=0.01` | 价格稳定的大盘股，简单估算 |
| **百分比滑点** | `slippage = price × pct`（如0.05%） | `slippage_pct=0.0005` | 不同价格水平资产，相对一致 |
| **波动率相关滑点** | `slippage = volatility_ratio × ATR`（如0.1×ATR） | `atr_ratio=0.1`, `atr_period=14` | 高波动时期自动扩大，更贴近实际 |
| **成交量相关滑点** | `slippage = base × (1 + order_size/volume_threshold)^exponent` | `volume_threshold`, `impact_exponent=0.5` | 大额订单，流动性冲击 |

#### 4.3.3 流动性冲击：成交量阈值、冲击成本估计

**流动性冲击简化模型**（参考Almgren-Chriss框架）：

$$\Delta P = \eta \cdot \sigma \cdot \left(\frac{X}{V}\right)^{0.6} + \gamma \cdot \frac{X}{V} \cdot \sigma \cdot \sqrt{T}$$

其中：$X$为订单量，$V$为日均成交量，$\sigma$为日波动率，$T$为执行时间，$\eta$、$\gamma$为市场冲击系数。

**实用简化**：设置`daily_volume_threshold`（如日成交量的10%），超过时触发`impact_multiplier`滑点乘数（如1.5×-3×线性递增），或强制分拆订单多日执行。

**Claude提示词（阶段4.3）：**

> "请为我实现**交易成本模拟模块TransactionCost**（`backtest/transaction_cost.py`），完整覆盖我的资产类别：1) **股票成本模型**（`StockCostModel`）：印花税0.001（仅卖出），佣金0.00025双向，`max(commission, 5)`最低收费，过户费0.00001双向（沪市），`calculate_cost(price, volume, direction)`返回`{'commission': float, 'tax': float, 'transfer_fee': float, 'total': float}`；2) **ETF成本模型**（`ETFCostModel`）：佣金0.00025双向，无印花税过户费，部分券商ETF佣金优惠可配置；3) **基金成本模型**（`FundCostModel`）：场内同ETF，场外申购费0.015（打一折后）、赎回费持有期分层（<7日0.015，<30日0.0075，<1年0.005，>2年0），`subscribe_fee`、`redemption_fee`参数化；4) **期货成本模型**（`FuturesCostModel`）：手续费按合约配置（如`{'IF': 2.3e-5, 'IC': 2.3e-5, 'IH': 2.3e-5, 'RB': 3.0}`），保证金比例`margin_rate`（默认0.12），`calculate_margin(price, volume, contract_multiplier)`返回占用保证金；5) **滑点模型**（`SlippageModel`）：基类定义`get_slippage_price(direction, price, volume, market_data)`，子类实现`FixedSlippage`（固定点数）、`PercentageSlippage`（百分比）、`VolatilitySlippage`（ATR相关，参数`atr_ratio=0.1`）、`VolumeImpactSlippage`（成交量冲击，参数`volume_threshold`, `impact_exponent=0.5`）；6) **Backtrader集成**：自定义`CommInfoBase`子类，重写`_getcommission`和`_getslippage`，在`Cerebro`中通过`broker.setcommission`和`broker.set_slippage_perc/fixed`配置；7) **成本分析工具**：`CostAnalyzer`统计回测中各项成本占比，可视化成本侵蚀收益曲线，敏感性分析不同成本假设下的绩效变化；8) **完整示例**：同一策略在不同成本假设（理想0成本、保守2倍成本、实际估算成本）下的回测对比，量化成本对策略可行性的影响。"

---
