
## 3. 第二阶段：学术文献解析与策略原型开发

### 3.1 PDF文献智能解析模块

#### 3.1.1 本地PDF读取：PyPDF2/pdfplumber结构化提取

PDF解析的技术选型需权衡提取精度与处理速度：

| 库 | 优势 | 局限 | 适用场景 |
|:---|:---|:---|:---|
| **PyPDF2** | 纯Python，轻量，无依赖 | 复杂排版解析弱，表格识别差 | 简单文本提取，快速原型 |
| **pdfplumber** | 基于PDFMiner，表格提取优秀，保留布局 | 速度较慢，内存占用较高 | 学术论文，含复杂表格和公式 |
| **PyMuPDF (fitz)** | 速度极快，渲染能力强，支持多种格式 | 依赖C库，安装复杂 | 扫描版PDF，需要页面渲染 |
| **pdf2image + OCR** | 处理扫描版文档 | 依赖Tesseract，识别准确率受限 | 历史文献，无文本层PDF |

**推荐组合**：pdfplumber为主（处理结构化学术论文），PyMuPDF为辅（处理复杂排版）。解析流程：PDF文件路径输入 → 文本分层提取（标题、摘要、正文、图表、参考文献） → 章节结构识别（基于字体大小和关键词匹配） → 关键段落定位（方法/Methodology、实验/Experiment、结果/Results）。

#### 3.1.2 核心内容识别：摘要、方法论、实验设计、结论

核心内容识别采用**规则与模型结合**的方法：

| 章节 | 识别特征 | 提取重点 |
|:---|:---|:---|
| **摘要/Abstract** | 文档开头，关键词"Abstract"/"摘要" | 研究目标、核心方法、主要发现、关键词 |
| **引言/Introduction** | 第1-2节，含文献综述 | 研究动机、问题定义、创新点声明 |
| **方法论/Methodology** | 含"Method"、"Model"、"Approach"、"Framework" | 数学模型、算法流程、特征定义、超参数 |
| **实验设计/Experiments** | 含"Data"、"Setup"、"Implementation"、"Backtest" | 数据集、样本划分、基准方法、评估指标 |
| **结果/Results** | 表格、图表集中区域 | 绩效指标、显著性检验、稳健性分析 |
| **结论/Conclusion** | 文档末尾，含"Conclusion"、"Summary" | 核心贡献、局限性、未来方向 |

#### 3.1.3 策略要素提取：信号生成逻辑、特征变量、模型架构、回测设置

策略要素的结构化提取是代码生成的基础：

| 要素类型 | 提取目标 | 结构化表示 |
|:---|:---|:---|
| **信号生成逻辑** | 入场/出场条件的数学表达式或规则描述 | 条件表达式树：`{"condition": "RSI < 30", "action": "BUY", "threshold": 30}` |
| **特征变量** | 输入模型的预测变量，含计算方法和参数 | 特征列表：`[{"name": "RSI_14", "formula": "RSI(close, 14)", "source": "TA-Lib"}]` |
| **模型架构** | 神经网络层结构、树模型参数、优化器配置 | 模型配置：`{"type": "LSTM", "layers": [64, 32, 16], "dropout": 0.2, "optimizer": "Adam"}` |
| **回测设置** | 时间范围、资产池、交易成本、再平衡频率 | 配置字典：`{"start": "2015-01-01", "end": "2023-12-31", "commission": 0.00025, "rebalance": "monthly"}` |

#### 3.1.4 改进点自动生成：假设检验、稳健性分析、实际交易约束

针对**中国市场日频交易**的实际约束，系统自动生成改进建议：

| 改进维度 | 典型问题 | 生成建议 | 预期效果 |
|:---|:---|:---|:---|
| **市场制度适配** | 论文基于美股T+0、无涨跌停 | 调整为T+1制度，信号延迟一日执行；添加涨跌停过滤，无法交易时跳过 | 避免模拟交易与实盘不可执行 |
| **数据频率调整** | 论文使用分钟数据，用户仅日频 | 评估日频有效性，调整特征计算窗口（如20日→5日），或建议获取分钟权限 | 保证策略在可用数据上可运行 |
| **成本精细化** | 论文忽略交易成本或假设过低 | 纳入印花税、佣金、滑点，重新测算绩效；评估成本敏感度 | 回测结果更接近实盘 |
| **流动性约束** | 论文使用全样本，未考虑冲击成本 | 添加市值过滤（如>50亿）、成交量阈值（如日成交额>1亿） | 避免小盘股不可执行 |
| **过拟合防范** | 超参数过多，样本内优化过度 | 建议简化模型，增加正则化；扩展样本外测试期；滚动窗口验证 | 提升策略稳健性 |
| **稳健性检验** | 单一市场/资产类别测试 | 建议跨市场（A股/港股）、跨资产（股票/期货）验证；不同市场状态（牛/熊/震荡） | 评估策略普适性 |

**Claude提示词（阶段3.1 - 文献阅读与改进建议）：**

> "请阅读我本地的PDF学术文献[文件路径：./papers/statistical_arbitrage_2024.pdf]，完成以下结构化分析任务：1) **论文元数据提取**：标题、作者、发表期刊/会议、年份、被引次数（如有）；2) **核心贡献识别**：用2-3句话概括主要创新，区分是新方法、新因子还是改进现有模型；3) **方法论框架详细提取**：信号生成逻辑的数学表达式或伪代码、完整特征变量列表（名称、计算方法、数据来源）、模型架构细节（层数、单元数、激活函数、损失函数、优化器）、训练流程（样本划分、批量大小、学习率、早停条件）、回测设计（时间范围、资产池、交易成本假设、基准对比、评估指标）；4) **关键假设与局限性分析**：列出论文明确声明的假设和隐含假设（如市场有效性程度、流动性充足），识别方法论弱点（过拟合风险、未来信息泄露、幸存者偏差、数据窥探）；5) **中国市场适配性评估**：针对T+1交易制度、涨跌停限制、卖空限制、散户主导的市场结构、数据可得性差异，分析直接应用的障碍；6) **改进建议生成**：提出3-5条具体改进，每条包含问题描述、改进方案、理论依据、预期效果、实现复杂度（高/中/低）；7) **输出格式**：结构化Markdown或JSON，包含`paper_info`、`methodology`、`assumptions_limitations`、`china_market_adaptation`、`improvement_proposals`字段，方便我审阅后回复'采纳建议X'或'直接实现原文'指导后续开发。请用中文回复，专业术语保留英文原文。"

### 3.2 统计套利策略优先实现

#### 3.2.1 配对交易：协整检验、价差建模、阈值触发

统计套利是量化交易的经典策略，**配对交易**是其核心形式。完整实现流程：

| 步骤 | 方法 | 技术细节 |
|:---|:---|:---|
| **配对筛选** | 同行业、市值相近、历史价格相关性高 | 计算60日相关系数，阈值>0.8；或距离法（价格序列欧氏距离最小） |
| **协整检验** | Engle-Granger两步法或Johansen检验 | EG法：OLS回归得残差，ADF检验残差平稳性（p<0.05）；Johansen法：多变量协整，适合多资产组合 |
| **价差建模** | 线性组合或比率，标准化为Z-score | `spread = P1 - β*P2`，`z_score = (spread - mean) / std`，β为OLS回归系数 |
| **信号生成** | 阈值突破触发，均值回归平仓 | 开仓：\|z_score\| > entry_threshold（通常2.0）；平仓：\|z_score\| < exit_threshold（通常0.5）或回归均值 |
| **风险管理** | 止损防止协整关系破裂 | 止损：\|z_score\| > stop_threshold（通常3.0）；时间止损：持仓超最大期限 |

#### 3.2.2 均值回归：Ornstein-Uhlenbeck过程、半衰期估计

**Ornstein-Uhlenbeck（OU）过程**是描述均值回归的随机微分方程：

$$dX(t) = \theta(\mu - X(t))dt + \sigma dW(t)$$

其中**θ为回归速度**，**μ为长期均值**，**σ为波动率**。关键衍生指标：

| 指标 | 公式 | 用途 |
|:---|:---|:---|
| **半衰期（Half-life）** | $t_{1/2} = \ln(2) / \theta$ | 价差回归一半所需时间，指导持仓周期预期 |
| **预期回归时间** | 基于当前偏离程度和θ估计 | 动态调整仓位大小或止损宽度 |
| **平稳性检验** | ADF检验p值 | 确认协整关系有效性，p>0.1时考虑退出 |

参数估计方法：将离散OU过程视为AR(1)模型 $X_t = a + bX_{t-1} + \epsilon_t$，则 $\theta = -\ln(b)$，$\mu = a/(1-b)$。

#### 3.2.3 多因子套利：因子暴露、残差分析、组合优化

多因子套利扩展配对交易至多资产场景：

| 环节 | 方法 | 输出 |
|:---|:---|:---|
| **因子模型构建** | PCA提取主成分，或Fama-French三因子 | 因子收益率序列、资产因子暴露β |
| **残差收益计算** | $ε_i = R_i - β_i'F$ | 特异性收益，剥离系统性风险 |
| **套利机会识别** | 残差偏离历史均值超过阈值 | 做多正残差、做空负残差的信号 |
| **组合优化** | 二次规划：最大化残差收益，约束因子中性、行业中性 | 最优权重向量，控制风险和换手率 |

**Claude提示词（阶段3.2 - 统计套利策略实现，采纳改进版）：**

> "基于我刚才审阅并认可的改进建议（改进点1：将止损阈值从固定2%调整为**ATR的2倍**以适应波动率变化；改进点2：添加**行业中性约束**，控制单一行业暴露不超过20%；改进点3：引入**协整关系动态监测**，ADF检验p值>0.1时强制平仓），请为我实现一个完整的统计套利配对交易策略：1) **项目结构**：`strategies/statistical_arbitrage/`目录，包含`__init__.py`、`pair_selection.py`（配对筛选）、`cointegration_test.py`（协整检验）、`signal_generator.py`（信号生成）、`position_sizer.py`（仓位管理）；2) **协整检验**：实现Engle-Granger两步法，包含ADF检验p值输出、半衰期估计、对冲比例β计算，支持同一行业内配对和自定义配对列表两种模式；3) **信号生成**：动态阈值机制，开仓阈值=μ ± k×σ（k默认为2.0，可配置），平仓阈值=μ ± 0.5×σ，止损阈值=μ ± 3.5×σ识别协整破裂，同时监控ADF p值动态退出；4) **止盈止损适配**：盈利10%启动**移动止盈**（最高点回撤5%触发），亏损**ATR×2**止损，持仓超过**25个交易日**强制平仓，支持分级减仓（盈利5%平30%，盈利10%平剩余）；5) **成本模拟**：股票佣金0.025%双向、印花税0.1%卖出、滑点0.01元或0.05%取较大者；6) **Backtrader集成**：继承`bt.Strategy`，`__init__`初始化指标（价差、Z-score、半衰期、ATR），`next`实现信号生成和订单管理，`notify_order`跟踪成交，完整日志记录；7) **测试与示例**：单元测试（`test_pair_selection.py`测试配对筛选逻辑，`test_signal.py`测试信号边界条件），示例运行脚本（`run_backtest.py`展示贵州茅台-五粮液配对2019-2024回测）；8) **文档**：详细docstring，类型注解，PEP 8规范，注释说明与原文方法的偏差。"

**Claude提示词（阶段3.2 - 统计套利策略实现，原版直接实现）：**

> "请直接按照PDF文献《Pairs Trading: Performance of a Relative-Value Arbitrage Rule》中的方法论，为我实现统计套利策略：1) **严格复现**：配对筛选标准（相关系数排序top 20）、协整检验流程（EG两步法，形成期252日）、价差计算方式（价格差而非收益率差）、交易阈值（±2标准差开仓，±0.5标准差平仓）；2) **明确标注**：论文未明确的参数使用合理默认值——止损阈值设为无（观察纯策略表现）、持仓周期无限制、交易成本假设为0（后续单独分析成本影响），并在代码注释中标注'原文未明确，采用默认值X'；3) **市场适配**：A股日频数据，使用tushare获取沪深300成分股，处理T+1制度（信号生成后次日开盘执行）、涨跌停过滤（无法交易时跳过）；4) **基础风控**：单一配对最大仓位10%，总配对数量不超过20个，防止过度集中；5) **输出规范**：Backtrader兼容的Strategy类，独立配置文件`strategy_config.yaml`隔离参数与代码，实现偏差说明文档`IMPLEMENTATION_NOTES.md`列出与原文的所有偏差及原因；6) **可运行交付**：完整代码、配置、示例数据、运行脚本，确保我能够直接执行并复现论文思路。"

### 3.3 机器学习预测模型开发

#### 3.3.1 特征工程：技术指标、宏观因子、文本情绪、交叉特征
### 特征工程层

- **tsfresh**：自动时序特征生成
    
- **TA-Lib**：172+技术指标
    
- **FeatureTools**：自动因子构建

系统化特征体系设计：

| 特征类别 | 具体指标 | 计算方法 | 适用模型 |
|:---|:---|:---|:---|
| **价格动量** | 收益率（1/5/10/20/60日）、价格位置（close/MA20） | `pct_change(periods=n)` | 全模型 |
| **技术指标** | MACD（DIF/DEA/柱状线）、RSI（6/12/24）、KDJ、布林带（上轨/中轨/下轨/带宽）、ATR（14） | TA-Lib库 | 全模型 |
| **波动特征** | 历史波动率、GARCH波动率、已实现波动率、波动率聚类 | `rolling(std).mean()`，`arch`库 | LSTM、Transformer |
| **量价关系** | OBV、量价相关系数、资金流入流出（大单/小单） | `ta.OBV`，自定义计算 | XGBoost、神经网络 |
| **宏观因子** | 10年期国债收益率、人民币汇率、VIX类代理变量、CPI/PPI/PMI | Tushare接口或外部数据，日期对齐 | 全模型 |
| **交叉特征** | 个股-指数相关性、行业轮动指标、动量×波动率、价格位置×成交量趋势 | 多项式组合，`PolynomialFeatures` | 树模型、神经网络 |

#### 3.3.2 模型选型：随机森林、XGBoost、LSTM、Transformer时序模型

| 模型 | 核心优势 | 主要局限 | 最佳实践 |
|:---|:---|:---|:---|
| **随机森林** | 训练极快，不易过拟合，特征重要性可解释 | 难以捕捉时序依赖，高维稀疏数据效果差 | 基线模型，特征筛选，快速验证 |
| **XGBoost/LightGBM** | 精度高，正则化完善，支持缺失值，自动特征选择 | 需仔细调参，时序意识弱 | 表格数据主力，网格搜索+早停 |
| **LSTM/GRU** | 显式建模长程时序依赖，适合非线性动态 | 训练慢，需大量数据，调参复杂，易梯度消失 | 多层堆叠，注意力机制，Dropout正则 |
| **Transformer** | 并行计算，全局注意力，长序列建模优异 | 数据饥渴，计算量大，位置编码设计关键 | Informer/Autoformer变体，长序列预测 |

#### 3.3.3 训练流程：滚动窗口、交叉验证、防止未来信息泄露

**时序数据训练的核心原则：严格按时间顺序划分，禁止随机打乱**。

| 方法 | 描述 | 适用场景 |
|:---|:---|:---|
| **滚动窗口（Rolling Window）** | 固定训练集长度，预测后滚动前进 | 模拟实盘定期再训练，适应市场变化 |
| **扩展窗口（Expanding Window）** | 训练集随时间累积扩大 | 数据量有限，希望利用全部历史信息 |
| **Purged K-Fold** | 训练集与验证集之间设置"清除期"，消除重叠 | 高频数据，样本间相关性强 |
| **Embargo** | 训练集结束后、验证集开始前预留空白期 | 进一步防止信息泄露 |

**关键禁忌清单**：
- ❌ 使用全局统计量（如全序列均值）后再分割
- ❌ 特征标准化在分割前进行
- ❌ 超参数选择在全样本上进行
- ✅ 所有特征计算仅使用当前及历史数据
- ✅ 标准化参数在训练集内拟合，应用于验证/测试集

#### 3.3.4 模型评估：方向准确率、收益率预测、信息系数(IC)

| 指标 | 定义 | 解读 | 阈值参考 |
|:---|:---|:---|:---|
| **方向准确率（DA）** | 预测方向与实际方向一致的比例 | 分类能力，但忽略幅度 | >55%有经济价值，>60%优秀 |
| **收益率预测MSE/MAE** | 预测收益率与实际收益率的误差 | 回归精度，绝对值参考 | 需结合方向指标 |
| **信息系数（IC）** | 预测值与实际收益的**秩相关系数** | 预测排序能力，量化核心指标 | \|IC\|>0.05有效，>0.1优秀 |
| **IC_IR** | IC均值 / IC标准差 | 预测稳定性，信息比率 | >0.5稳定，>1.0非常稳定 |
| **分位数收益** | 按预测值分5组，检验组间收益单调性 | 预测区分度，实际可操作性 | 最高组显著跑赢最低组 |
| **回测夏普比率** | 纳入交易成本后的风险调整后收益 | 最终经济价值 | >1.0可接受，>1.5良好，>2.0优秀 |

**Claude提示词（阶段3.3）：**

> "请为我设计一个基于**LSTM**的价格走势预测模型，完整实现以下模块：1) **特征工程**（`features/engineering.py`）：输入OHLCV，计算技术指标（MA5/10/20/60、MACD、RSI14、布林带宽度、ATR14），宏观因子（10年期国债收益率、人民币汇率，**滞后1日处理**避免未来信息），交叉特征（个股-行业收益差、个股-指数波动率比），输出约30维特征矩阵；2) **数据管道**（`data/pipeline.py`）：实现**滚动窗口训练**，训练集756日（3年）、验证集252日（1年）、测试集252日，窗口步长63日（季度滚动），严格避免未来信息泄露——所有特征使用`shift(1)`确保滞后，标准化参数仅在训练集内拟合；3) **LSTM模型**（`models/lstm_model.py`）：架构为LSTM[64,32] + Dropout(0.2) + Dense[16] + 输出层，预测未来**5日对数收益率**（回归）或**涨跌方向**（分类），损失函数分别为MSE或Focal Loss（处理类别不平衡）；4) **训练流程**：Adam优化器，学习率1e-3，ReduceLROnPlateau调度，EarlyStopping（验证损失5轮不下降则停，恢复最佳权重），模型版本管理（保存结构、权重、训练配置、性能指标）；5) **Backtrader集成**（`strategies/ml_strategy.py`）：`MLStrategy`类接收模型预测，生成交易信号——预测收益>2%且模型置信度>0.6时买入，预测收益<-2%或持仓收益>10%时卖出；6) **超参数调优建议**：学习率范围[1e-4, 1e-2]，LSTM单元数[32, 64, 128]，Dropout率[0.1, 0.3, 0.5]，lookback窗口[20, 60, 120]，使用Optuna贝叶斯优化；7) **完整交付**：可运行代码、配置文件、训练脚本、回测示例、性能评估报告（IC、IC_IR、方向准确率、回测夏普）。"
