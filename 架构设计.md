
## 1. 系统架构与核心模块规划

### 1.1 整体架构设计

#### 1.1.1 模块划分原则：数据层、策略层、回测层、风控层、可视化层

构建一套完整的量化交易分析系统，首要任务是确立清晰的分层架构。基于用户需求——支持个股、ETF、公募基金、期货四类资产，以日频手工交易为主，融合机器学习预测算法——系统应采用经典的五层架构模式：**数据层**负责原始数据的获取、清洗与持久化；**策略层**承载学术文献解析、信号生成与模型推理；**回测层**实现历史模拟与绩效评估；**风控层**管理止盈止损、仓位控制与成本模拟；**可视化层**通过Streamlit提供交互式分析界面。

这种分层设计确保了各模块职责单一、接口清晰，便于后续迭代与扩展。数据层与策略层解耦，使得新数据源或新资产类别的接入无需改动策略逻辑；回测层与风控层的分离，则允许用户灵活配置风险规则而不影响核心回测引擎。特别需要注意的是，用户强调日频手工交易，这意味着系统在实时性要求上可以适当放宽，但在数据质量和策略稳健性上需要更高标准。

#### 1.1.2 技术栈选型：Python生态、Tushare数据接口、MySQL本地存储、Streamlit前端

技术栈的选择需兼顾功能完备性与开发效率。经过综合评估，推荐以下技术组合：

| 层级   | 核心技术                                 | 选型理由                                                               |
| :--- | :----------------------------------- | :----------------------------------------------------------------- |
| 编程语言 | **Python 3.10+**                     | 量化金融生态最完善，Tushare、Backtrader、scikit-learn、PyTorch/TensorFlow等库成熟稳定 |
| 数据获取 | **Tushare Pro API** 为主，可以辅助一些其他数据    | 覆盖A股全市场数据，含股票、基金、期货、期权，支持日线/分钟线/Tick，性价比高                          |
| 数据存储 | **MySQL 8.0+**                       | 用户已有本地部署，关系型数据库适合结构化金融数据，支持事务与复杂查询                                 |
| 数据处理 | **Pandas + NumPy**                   | 金融时间序列处理标准工具，与Tushare、MySQL无缝集成                                    |
| 机器学习 | **scikit-learn + XGBoost + PyTorch** | 传统ML与深度学习全覆盖，支持时序预测与分类任务                                           |
| 回测框架 | **Backtrader**（主）+ **VectorBT**（辅助）  | 功能丰富、文档完善，支持自定义DataFeed与Analyzer，社区活跃                              |
| 可视化  | **Streamlit + Plotly**               | 快速构建数据应用，Python原生，部署简便                                             |
| 开发工具 | Cursor                               | 代码编辑、交互探索、版本控制三位一体                                                 |

该技术栈的核心优势在于生态协同：Tushare获取的数据可直接转为Pandas DataFrame，经特征工程后输入scikit-learn或PyTorch模型，模型预测结果通过Backtrader进行回测，最终绩效数据由Streamlit可视化呈现，全流程Python原生实现，避免了多语言切换的摩擦成本。

#### 1.1.3 资产类别覆盖：个股、ETF、公募基金、期货的统一数据模型

四类资产在交易机制、成本结构、数据字段上存在显著差异，需设计统一而灵活的数据模型：

| 资产类别 | 核心特征 | 特殊处理需求 |
|:---|:---|:---|
| **个股** | T+1交易，±10%涨跌停（ST±5%），印花税0.1%卖出 | 复权处理、停牌标记、ST标识 |
| **ETF** | T+1为主（部分跨境T+0），无印花税，佣金较低 | 折溢价计算、申赎机制、跟踪误差 |
| **公募基金** | 场外申赎（T+1确认）或场内交易，费率结构复杂 | 净值披露延迟、大额申赎限制、费率分层 |
| **期货** | T+0交易，保证金制度，合约到期 | 主力合约切换、移仓换月、基差跟踪、保证金动态计算 |

统一数据模型的核心设计：**基础信息表**通过`asset_type`字段区分类别，存储代码、名称、上市日期、交易所等通用属性；**行情表**采用一致的OHLCV字段结构，通过`adj_factor`字段支持复权计算；**交易记录表**扩展`cost_breakdown` JSON字段，记录不同资产的手续费明细。这种设计使得策略代码可通过统一接口访问任意资产，仅在风控与成本模块根据`asset_type`差异化处理。

#### 1.1.4 交易频率适配：日频数据为主，分钟级数据扩展预留

用户明确以**日频手工交易**为主，但系统架构需预留分钟级扩展能力：

| 维度 | 日频数据 | 分钟级数据（预留） |
|:---|:---|:---|
| 数据量 | 单资产年约250条，全市场约125万条/年 | 单资产年约6万条（1分钟），数据量放大240倍 |
| 存储策略 | MySQL主表，按年分区 | 独立表或时序数据库（InfluxDB/TimescaleDB） |
| 特征工程 | 技术指标、宏观因子、基本面数据 | 日内模式、订单流、高频因子 |
| 回测撮合 | 收盘价撮合 | TWAP/VWAP模拟、订单簿简化 |
| 适用策略 | 趋势跟踪、均值回归、机器学习预测 | 日内择时、高频套利、事件驱动 |

数据层设计应支持多时间粒度：日行情表（`daily_kline`）为主表，分钟行情表（`minute_kline`）为扩展表，两者通过`symbol`+`trade_date`/`trade_time`复合索引关联。策略层通过`resample`或`agg`方法实现时间粒度转换，回测层则根据数据频率自动调整撮合逻辑。

### 1.2 核心功能矩阵

#### 1.2.1 数据获取与清洗模块

数据质量是量化系统的生命线。该模块需实现：**Tushare API封装与限流管理**，支持多资产类别的批量下载；**数据完整性校验**，包括价格非负、成交量合理、时间序列连续等规则；**复权处理**，提供前复权（分析用）与后复权（回测用）两种模式；**异常值检测与处理**，如涨跌停日的价格锁定、停牌日的数据填充。清洗后的数据写入MySQL，建立分区表按年份拆分，确保查询性能。模块还需支持**增量更新机制**，通过`last_update`字段记录各资产最新数据日期，避免全量重复下载。

#### 1.2.2 特征工程与机器学习模型开发模块

机器学习预测算法的核心是特征与模型的协同设计：

| 特征类别 | 具体内容 | 计算方法 |
|:---|:---|:---|
| **技术指标** | 动量、均值回归、波动率、成交量 | TA-Lib库，多周期参数（5/10/20/60日） |
| **宏观因子** | 利率、汇率、商品指数、市场情绪 | Tushare接口或外部数据源，日期对齐 |
| **量价特征** | 价格分位数、成交量异动、资金流向 | 自定义计算，滚动窗口统计 |
| **交叉特征** | 行业×市值分层、个股-指数相关性、波动率-成交量 | 多项式组合、非线性变换 |

模型开发支持两类范式：**传统机器学习**（随机森林、XGBoost、LightGBM）适合中小样本、可解释性强的场景；**深度学习**（LSTM、GRU、Transformer）适合长序列依赖、高维特征的场景。关键设计在于训练流程的**时序交叉验证**：采用滚动窗口（rolling window）或扩展窗口（expanding window）划分训练/验证集，严格禁止未来信息泄露。模型输出为预测概率或收益率分布，经校准后转化为交易信号。

#### 1.2.3 多策略回测引擎（单资产、多策略并行）

回测引擎需满足**"单资产、多策略"**的核心需求——同一资产可同时运行多个策略，结果独立评估、对比分析：

| 组件 | 功能 | 技术实现 |
|:---|:---|:---|
| **DataFeed** | 从MySQL读取数据，转换为Backtrader格式 | 自定义`PandasData`子类，支持日期范围筛选 |
| **Strategy基类** | 统一接口：初始化、逐日决策、订单回调 | 抽象基类`BaseStrategy`，子类实现`generate_signal` |
| **撮合引擎** | 日频收盘价撮合，支持限价单与止损单 | Backtrader内置`Broker`，自定义滑点模型 |
| **Analyzer** | 绩效指标计算 | 内置`SharpeRatio`、`DrawDown`，自定义`CalmarRatio` |
| **多策略聚合** | 并行运行、结果对比、持久化存储 | 策略参数化实例化，结果写入`backtest_results`表 |

#### 1.2.4 风险管理系统（止盈止损、仓位控制）

| 机制类型 | 具体实现 | 适用场景 |
|:---|:---|:---|
| **固定比例止盈/止损** | 盈利10%止盈，亏损5%止损 | 简单直接，纪律性强 |
| **移动止盈（Trailing Stop）** | 最高价回撤5%触发 | 趋势跟踪，让利润奔跑 |
| **波动率止损（ATR Stop）** | 2×14日ATR作为止损距离 | 自适应市场波动 |
| **时间止损** | 持仓超20日强制平仓 | 均值回归，防止资金占用 |
| **复合条件** | 多条件"或"触发，优先级排序 | 灵活组合，全面风控 |
| **部分平仓** | 分级止盈：5%平50%，10%平剩余 | 锁定利润，保留敞口 |
| **Kelly公式仓位** | 最优仓位 = (bp-q)/b | 理论最优，需保守调整 |
| **风险平价** | 各资产风险贡献相等 | 组合分散，降低尾部风险 |
| **最大回撤控制** | 回撤超15%减仓，修复后恢复 | 保护本金，反周期调整 |

#### 1.2.5 交易成本模拟（手续费、滑点）

| 资产类别 | 成本结构 | 备注 |
|:---|:---|:---|
| **股票** | 印花税0.1%（卖出）、佣金0.025%双向、过户费0.001% | 佣金最低5元 |
| **ETF** | 佣金0.025%双向，无印花税 | 部分券商ETF佣金优惠 |
| **基金** | 场外：申购费0.1%-1.5%、赎回费持有期递减；场内：同ETF | 7日内赎回惩罚性费率1.5% |
| **期货** | 手续费按合约（固定值或比例）、保证金占用利息 | 日内平仓优惠 |

**滑点模型**：固定滑点（0.01元）、百分比滑点（0.05%）、波动率相关滑点（0.1×ATR）。大额交易考虑流动性冲击：订单量超日均成交量10%时，滑点乘数递增。

#### 1.2.6 学术文献解析与策略实现模块

这是系统的**差异化能力**所在。工作流程设计：

```
PDF文献 → 结构化提取 → 策略要素识别 → 改进建议生成 → 用户决策 → 代码实现 → 回测验证
```

| 阶段 | 技术实现 | 输出 |
|:---|:---|:---|
| **PDF读取** | `pdfplumber`/`PyMuPDF`提取文本、表格、公式 | 章节结构化的原始内容 |
| **内容识别** | 规则匹配（关键词+位置）+ NLP分类 | 摘要、方法论、实验设计、结论 |
| **要素提取** | 正则表达式 + 语义解析 | 信号逻辑、特征变量、模型架构、回测参数 |
| **改进生成** | 规则引擎：中国市场约束匹配 | 3-5条具体改进建议，含理论依据与预期影响 |
| **用户决策** | 交互确认 | 采纳改进 / 直接复现 / 参考思路 |
| **代码生成** | 模板填充 + 上下文感知 | 符合项目规范的Python策略代码 |

#### 1.2.7 Streamlit可视化Dashboard

| 页面 | 核心功能 | 可视化组件 |
|:---|:---|:---|
| **策略绩效页** | 累计收益、回撤曲线、关键指标 | Plotly交互式图表，对数/线性切换，基准对比 |
| **交易分析页** | 买卖点标注、持仓周期、盈亏分布 | K线图+标记，直方图，箱线图，明细表格 |
| **模型诊断页** | 预测准确率、特征重要性、残差分析 | 时序图，条形图，散点图，混淆矩阵 |
| **参数调优页** | 实时调参、重新回测、结果对比 | 滑块控件，并排图表，配置保存 |
| **实时监控页** | 最新预测、当前持仓、风险敞口 | 数据卡片，预警提示，组合汇总 |

---