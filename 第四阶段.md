第四阶段：Streamlit可视化Dashboard

### 5.1 核心可视化组件

#### 5.1.1 策略绩效概览：累计收益曲线、回撤曲线、月度收益热力图

| 组件 | 实现技术 | 交互功能 | 关键洞察 |
|:---|:---|:---|:---|
| **累计收益曲线** | Plotly `go.Scatter`，对数/线性切换 | 缩放、平移、悬停显示日期收益，基准叠加 | 长期趋势、相对强弱、关键转折点 |
| **回撤曲线** | 填充区域图，峰值-谷值标注 | 最大回撤区间高亮，持续时间显示 | 风险集中期、恢复能力、策略韧性 |
| **月度收益热力图** | Plotly `go.Heatmap`或表格+颜色编码 | 年份筛选，月份排序，统计摘要 | 季节性模式、连续盈亏月份、极端月份识别 |
| **关键指标卡片** | Streamlit `metric` + `columns`布局 | 实时计算，对比基准，趋势箭头 | 一目了然的风险收益概览 |

**指标卡片设计**：年化收益率（绿色/红色趋势箭头）、年化波动率、夏普比率（核心指标放大显示）、最大回撤（红色警示）、Calmar比率、胜率、盈亏比、交易次数、平均持仓天数。

#### 5.1.2 交易分析：买卖点标注、持仓周期分布、盈亏分布

| 组件 | 实现技术 | 数据展示 |
|:---|:---|:---|
| **K线+买卖点图** | `mplfinance`或Plotly Candlestick + 散点标记 | 绿色三角形买入，红色三角形卖出，悬停显示交易详情 |
| **持仓周期分布** | Plotly直方图，核密度估计叠加 | 识别策略风格（短线<5日、中线5-20日、长线>20日） |
| **盈亏分布** | 分组直方图（盈利/亏损分离），箱线图 | 右偏分布理想，识别肥尾、极端盈亏 |
| **连续盈亏分析** | 条形图，最长连续序列标注 | 评估策略"韧性"，连续亏损期是否可接受 |
| **交易明细表格** | `st.dataframe` + 排序筛选 | 日期、代码、方向、价格、数量、成本、盈亏、持仓天数、退出原因 |

#### 5.1.3 模型诊断：预测准确率、特征重要性、残差分析

| 组件 | 适用模型 | 诊断目的 |
|:---|:---|:---|
| **预测准确率时序** | 分类模型 | 识别性能衰减期，触发再训练 |
| **IC/IR滚动曲线** | 回归/分类模型 | 预测稳定性，信息比率趋势 |
| **特征重要性条形图** | 树模型 | 特征工程指导，冗余识别 |
| **注意力权重热图** | Transformer | 模型关注的时间点和特征 |
| **预测-实际散点图** | 回归模型 | 校准度检验，系统性偏差 |
| **残差分布与自相关** | 全模型 | 模型假设检验，改进方向 |
| **分位数收益单调性** | 排序模型 | 预测区分度，实际可操作性 |

#### 5.1.4 实时信号监控：最新预测、当前持仓、风险敞口

| 组件 | 数据来源 | 更新频率 | 预警机制 |
|:---|:---|:---|:---|
| **最新预测面板** | 模型推理API | 每日收盘后 | 置信度低于阈值标黄，方向反转标红 |
| **当前持仓表格** | 回测状态或实盘接口 | 实时/日频 | 止盈止损临近提醒，持仓超期警告 |
| **风险敞口汇总** | 持仓聚合计算 | 实时 | 行业集中度>30%警告，单资产>20%警告 |
| **组合指标卡片** | 实时计算 | 实时 | 总仓位、多空净敞口、Beta、VaR(95%) |
| **市场异常监测** | 市场数据流 | 实时 | 指数大幅波动、个股异动、流动性紧张 |

**Claude提示词（阶段5.1）：**

> "请为我设计一个**Streamlit量化交易Dashboard**（`visualization/app.py`），完整实现以下页面：1) **策略绩效页**（`pages/performance.py`）：顶部关键指标卡片（年化收益、夏普、最大回撤、Calmar），Plotly交互式累计收益曲线（对数/线性切换，基准叠加），回撤曲线（最大回撤区间高亮），月度收益热力图（年份×月份，颜色编码，统计摘要）；2) **交易明细页**（`pages/trades.py`）：`mplfinance`或Plotly K线图+买卖点标注（绿色买入/红色卖出三角形，悬停详情），持仓周期分布直方图，盈亏分布分组图，连续盈亏分析，可排序筛选的交易明细表格（日期、代码、方向、价格、数量、成本、盈亏、持仓天数、退出原因）；3) **模型分析页**（`pages/model_diagnosis.py`，机器学习策略适用）：预测准确率时序图，IC/IR滚动曲线，特征重要性条形图（树模型）或注意力热图（Transformer），预测-实际散点图+回归线，残差分布与自相关检验，分位数收益单调性验证；4) **参数调优页**（`pages/optimization.py`）：侧边栏滑块调整策略参数（如均线周期、阈值、仓位比例），'运行回测'按钮触发后台计算（`st.spinner`进度显示，`@st.cache_data`缓存避免重复），结果区域并排展示参数组合对比（收益曲线叠加，指标表格，雷达图）；5) **状态管理**：`st.session_state`持久化用户选择（当前策略、日期范围、参数配置），多页面数据共享，避免重复加载；6) **性能优化**：大数据集使用`st.cache_data`和`st.cache_resource`，数据库查询结果缓存，Plotly图表`use_container_width=True`自适应布局。"

### 5.2 交互式回测配置

#### 5.2.1 参数面板：策略选择、时间范围、初始资金、交易成本

| 参数类别 | 控件类型 | 动态加载 | 验证规则 |
|:---|:---|:---|:---|
| **策略选择** | `st.selectbox`下拉菜单 | 从`strategies/`目录扫描Python文件，提取类名和docstring | 必须选择有效策略类 |
| **日期范围** | `st.date_input`双日期选择器 | 限制在数据库可用数据范围内 | 结束日期>开始日期，区间≥1年 |
| **初始资金** | `st.number_input`数值输入 | 默认100万，步长10万 | ≥10万，≤1亿 |
| **交易成本** | `st.expander`折叠面板内嵌滑块 | 股票/ETF/基金/期货切换显示相关参数 | 费率≥0，滑点≥0 |
| **策略专属参数** | 动态生成（根据策略`params`定义） | 反射提取参数名、类型、默认值、范围 | 类型检查，范围约束 |

#### 5.2.2 实时回测触发：按钮控制、进度显示、结果缓存

| 环节 | 实现技术 | 用户体验优化 |
|:---|:---|:---|
| **触发按钮** | `st.button("运行回测", type="primary")` | 禁用状态防止重复点击，点击后变"运行中..." |
| **进度显示** | `st.progress`进度条 + `st.spinner`旋转动画 | 分阶段显示：数据加载→特征计算→回测执行→结果生成 |
| **后台执行** | `threading`或`concurrent.futures`避免阻塞UI | 支持取消操作，异常捕获并友好提示 |
| **结果缓存** | `@st.cache_data(ttl=3600)`按参数哈希缓存 | 相同参数秒级返回，参数变化自动失效 |
| **结果展示** | 指标卡片 + 图表 + 表格，折叠面板组织 | 关键结论置顶，详细数据可展开 |

#### 5.2.3 多策略对比：并排展示、指标雷达图、显著性检验

| 对比维度 | 可视化方式 | 统计检验 |
|:---|:---|:---|
| **收益曲线叠加** | 多线Plotly图表，不同颜色区分 | 目视检验趋势一致性 |
| **关键指标表格** | `st.dataframe`高亮最优值 | 夏普比率差异的t检验 |
| **雷达图多维度** | Plotly `go.Scatterpolar`，归一化指标 | 综合评估策略能力轮廓 |
| **相关性矩阵** | 热力图，聚类排序 | 策略分散化效果 |
| **参数敏感性** | 热力图或3D曲面，单参数/双参数扫描 | 稳健性评估 |

**Claude提示词（阶段5.2）：**

> "请为我实现Streamlit中的**交互式回测配置功能**，完整模块`visualization/backtest_config.py`：1) **动态策略加载**：扫描`strategies/`目录，提取继承`BaseStrategy`的类，构建`{类名: (类, 参数定义)}`字典，`st.selectbox`选择后动态渲染专属参数控件（`st.slider`/`st.number_input`/`st.selectbox`，根据参数类型和范围）；2) **全局参数面板**：侧边栏`st.sidebar`布局，日期范围选择器（限制数据库实际范围），初始资金输入（默认100万，验证≥10万），交易成本折叠面板（资产类别切换，股票显示印花税/佣金/过户费滑块，ETF简化，期货显示手续费/保证金）；3) **回测执行流程**：'运行回测'按钮→`st.spinner`显示进度→后台`Cerebro`执行（`threading`避免阻塞）→进度条更新（0%数据加载，30%特征计算，60%回测执行，90%结果生成）→结果缓存到`st.session_state`；4) **结果展示区域**：关键指标卡片（与基准对比，趋势箭头），Plotly收益曲线（多策略叠加，对数/线性切换），交易统计表格（排序筛选，导出CSV），'保存配置'按钮写入MySQL`saved_configs`表（配置名、参数JSON、回测ID、创建时间）；5) **多策略对比**：多选策略后批量运行，并排展示指标雷达图（年化收益、夏普、最大回撤、胜率、盈亏比，归一化0-1），统计显著性检验（夏普差异t检验p值），'生成对比报告'按钮导出PDF；6) **性能优化**：`@st.cache_data`缓存数据加载和特征计算，`@st.cache_resource`缓存模型实例，`st.experimental_memo`缓存中间结果，大数据集使用`pyarrow`加速。"

---
