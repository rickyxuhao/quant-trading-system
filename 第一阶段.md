2. 第一阶段：基础设施与数据体系建设

### 2.1 开发环境配置

#### 2.1.1 Python环境：3.10+，依赖管理（Poetry推荐）

Python 3.10+版本提供了模式匹配、更精确的类型提示等现代特性，且与主流科学计算库兼容良好。**依赖管理强烈推荐Poetry**：相比传统`requirements.txt`，Poetry通过`pyproject.toml`集中管理依赖版本与开发配置，自动生成锁定文件`poetry.lock`确保环境可复现，并内置虚拟环境管理。

核心依赖清单：

| 类别 | 库名 | 版本约束 | 用途 |
|:---|:---|:---|:---|
| 数据处理 | `pandas` | ^2.0 | 时间序列数据处理 |
| 数值计算 | `numpy` | ^1.24 | 底层数值运算 |
| 数据接口 | `tushare` | ^1.2.89 | 金融数据API |
| 数据库 | `pymysql`, `sqlalchemy` | ^1.1, ^2.0 | MySQL连接与ORM |
| 机器学习 | `scikit-learn`, `xgboost`, `lightgbm` | ^1.3, ^2.0, ^4.0 | 传统ML算法 |
| 深度学习 | `torch` 或 `tensorflow` | ^2.0 / ^2.13 | 神经网络 |
| 回测框架 | `backtrader`, `vectorbt` | ^1.9.78, ^0.24 | 事件驱动与向量化回测 |
| 技术指标 | `TA-Lib` | ^0.4 | 技术分析指标 |
| 可视化 | `streamlit`, `plotly` | ^1.28, ^5.18 | 交互式Dashboard |
| 工具 | `pydantic`, `loguru`, `python-dotenv` | ^2.5, ^0.7, ^1.0 | 配置验证、日志、环境变量 |

#### 2.1.2 数据库环境：MySQL 8.0+，连接池配置

MySQL 8.0引入的**窗口函数**、**CTE（公用表表达式）**、**JSON数据类型增强**等特性，显著提升了时序数据查询的表达能力。关键配置参数：

| 参数 | 建议值 | 说明 |
|:---|:---|:---|
| `innodb_buffer_pool_size` | 物理内存的70% | 缓存数据和索引 |
| `innodb_log_file_size` | 1GB | 大事务支持 |
| `max_connections` | 200 | 连接池上限 |
| `wait_timeout` | 28800 | 连接空闲超时 |
| `time_zone` | '+08:00' | 东八区，匹配A股交易时间 |

SQLAlchemy连接池配置：`pool_size=10`（基础连接数）、`max_overflow=20`（峰值额外连接）、`pool_recycle=3600`（连接回收周期防止超时断开）、`pool_pre_ping=True`（连接前健康检查）。

#### 2.1.3 开发工具链：VS Code + Jupyter + Git版本控制

| 工具 | 配置要点 | 用途 |
|:---|:---|:---|
| **VS Code** | Python扩展、Pylance（严格类型检查）、Jupyter扩展、GitLens | 代码编辑、调试、版本可视化 |
| **Jupyter Lab** | `%load_ext autoreload`实现模块热更新 | 探索性数据分析、模型原型 |
| **Git** | Git Flow分支模型：`main`/`develop`/`feature/*`/`strategy/*` | 版本控制、策略迭代管理 |

**Git工作流建议**：`main`分支保持稳定可运行版本；`develop`分支集成日常开发；`feature/*`分支承载新功能；`strategy/*`分支隔离策略实验，便于A/B测试。提交信息遵循Conventional Commits规范，便于自动生成CHANGELOG。

**Claude提示词（阶段2.1）：**

> "请为我设计一个量化交易系统的Python项目结构，包含：1) 清晰的目录组织（`data/`、`strategies/`、`backtest/`、`risk/`、`visualization/`、`models/`、`utils/`、`config/`、`tests/`）；2) 依赖管理文件`pyproject.toml`，包含tushare、pymysql、pandas、numpy、scikit-learn、tensorflow/pytorch、backtrader、streamlit等核心库，并指定版本约束；3) 配置文件模板（`.env.example`包含数据库连接、Tushare token、交易参数），以及`config.py`加载配置；4) 日志系统初始化代码，使用`loguru`实现分级日志（DEBUG/INFO/WARNING/ERROR），按日期轮转，保留30天；5) 项目入口`main.py`和CLI命令行接口。要求代码符合PEP 8规范，类型注解完整，支持后续模块化扩展。"

### 2.2 MySQL数据库设计与实现

#### 

#### 2.2.3 索引优化：时间序列分区、复合索引设计

金融时间序列数据的查询模式具有明显特征，索引策略需针对性优化：

| 查询场景 | 索引策略 | 预期效果 |
|:---|:---|:---|
| 单资产历史数据查询 | `(symbol, trade_date)` 复合主键 | 避免全表扫描，O(log n)定位 |
| 跨资产某日数据查询 | `(trade_date, symbol)` 辅助索引 | 快速获取截面数据 |
| 时间范围聚合查询 | 分区裁剪 + 覆盖索引 | 仅扫描相关分区，减少IO |
| 策略绩效按时间筛选 | `(start_date, end_date)` 索引 | 快速定位回测区间 |
| 交易记录按策略检索 | `(strategy_name, trade_date)` 复合索引 | 策略归因分析加速 |

**分区维护**：定期检查分区使用情况，通过`ALTER TABLE ... ADD PARTITION`预创建未来分区，避免写入失败。

#### 2.2.4 数据更新机制：增量同步、数据校验、异常处理

数据 pipeline 可靠性设计：

| 阶段 | 机制 | 实现 |
|:---|:---|:---|
| **增量检测** | 记录`last_update`时间戳，查询差集 | `SELECT MAX(trade_date) FROM daily_kline WHERE symbol = ?` |
| **批量获取** | Tushare分页API，控制单次数据量 | `pro.daily(ts_code=symbol, start_date=start, end_date=end)` |
| **数据校验** | 多维度规则检查 | 价格范围、涨跌幅合理性、成交量匹配、时间连续性 |
| **事务写入** | 单批次原子操作 | `BEGIN ... COMMIT/ROLLBACK`，`INSERT ... ON DUPLICATE KEY UPDATE` |
| **异常处理** | 分级响应策略 | 网络错误→指数退避重试；数据异常→记录日志跳过；严重错误→告警通知 |

**Claude提示词（阶段2.2）：**

> "请为我设计一套支持多资产类别（股票、ETF、基金、期货）的MySQL数据库表结构，要求：6) 提供完整的CREATE TABLE语句、索引优化建议、分区维护脚本；7) 说明各表的设计决策和查询优化思路，参考金融时间序列数据库最佳实践。"

### 2.3 Tushare数据接入层开发

#### 2.3.1 API封装：Pro版接口统一调用、限流处理、异常重试

Tushare Pro采用**积分制限流**，不同接口消耗积分各异，免费用户有严格的调用频率限制。封装层核心设计：

```python
import time
from functools import wraps
from typing import Callable, Any
import tushare as ts
from loguru import logger

class TushareRateLimiter:
    """令牌桶限流器"""
    def __init__(self, max_calls: int = 200, period: int = 60):
        self.max_calls = max_calls  # 每分钟最大调用次数
        self.period = period
        self.calls = []
    
    def __call__(self, func: Callable) -> Callable:
        @wraps(func)
        def wrapper(*args, **kwargs):
            now = time.time()
            # 清理过期记录
            self.calls = [c for c in self.calls if now - c < self.period]
            if len(self.calls) >= self.max_calls:
                sleep_time = self.period - (now - self.calls[0])
                logger.warning(f"Rate limit reached, sleeping {sleep_time:.2f}s")
                time.sleep(max(0, sleep_time))
            self.calls.append(now)
            return func(*args, **kwargs)
        return wrapper

class TushareDataClient:
    """Tushare数据客户端"""
    def __init__(self, token: str, rate_limit: int = 200):
        self.pro = ts.pro_api(token)
        self.rate_limiter = TushareRateLimiter(rate_limit)
    
    @retry(stop=stop_after_attempt(3), wait=wait_exponential(multiplier=1, min=2, max=10))
    @TushareRateLimiter(max_calls=200)
    def fetch_daily(self, ts_code: str = None, trade_date: str = None,
                    start_date: str = None, end_date: str = None) -> pd.DataFrame:
        """获取日线数据，自动限流与重试"""
        try:
            df = self.pro.daily(ts_code=ts_code, trade_date=trade_date,
                               start_date=start_date, end_date=end_date)
            return df
        except Exception as e:
            logger.error(f"Tushare API error: {e}")
            raise
    
    def fetch_fund_daily(self, ts_code: str, start_date: str, end_date: str) -> pd.DataFrame:
        """获取基金净值数据"""
        return self.pro.fund_nav(ts_code=ts_code, start_date=start_date, end_date=end_date)
    
    def fetch_fut_daily(self, ts_code: str, start_date: str, end_date: str) -> pd.DataFrame:
        """获取期货日线数据"""
        return self.pro.fut_daily(ts_code=ts_code, start_date=start_date, end_date=end_date)
```

关键特性：**装饰器链式组合**（限流+重试）、**指数退避重试**（2s, 4s, 8s）、**详细日志记录**便于问题追溯。

#### 2.3.2 数据获取调度：全量初始化、增量更新、定时任务

| 场景 | 策略 | 实现 |
|:---|:---|:---|
| **全量初始化** | 历史数据批量获取，分页处理 | 按资产循环，单次最多8000条，多线程并行 |
| **增量更新** | 每日盘后检测更新 | APScheduler定时任务，交易日17:30执行 |
| **复权因子同步** | 月度更新或实时检测变化 | 与行情数据同批次获取，保证一致性 |
| **主力合约切换** | 期货连续合约维护 | 识别成交量最大合约，生成映射关系 |

**调度配置示例**：
```python
from apscheduler.schedulers.background import BackgroundScheduler

scheduler = BackgroundScheduler()
scheduler.add_job(
    func=daily_update_job,
    trigger='cron',
    hour=17, minute=30,
    day_of_week='mon-fri',
    id='daily_market_data_update'
)
scheduler.start()
```

#### 2.3.3 数据清洗与标准化：缺失值处理、复权计算、字段统一

| 问题类型 | 检测方法 | 处理策略 |
|:---|:---|:---|
| 价格缺失（停牌） | `volume == 0` 或 `is_suspended == 1` | 价格前向填充，成交量置0，标记停牌 |
| 价格异常跳变 | `abs(change_pct) > 20%`（非ST） | 检查复权因子，确认除权除息 |
| 成交量异常 | `volume > 10 * MA(volume, 20)` | 标记异常，人工复核或剔除 |
| 时间序列断裂 | 相邻交易日日期差 > 1 | 标记缺失日期，策略层处理 |
| 复权因子断层 | 相邻日复权因子变化率异常 | 重新获取完整复权历史 |

**复权计算**：
- **前复权**：`adj_price = raw_price * adj_factor / latest_adj_factor`（分析用，保证历史连续性）
- **后复权**：`adj_price = raw_price * adj_factor / first_adj_factor`（回测用，反映真实收益）

#### 2.3.4 MySQL持久化：批量插入、事务管理、去重机制

高性能写入策略：

| 方法 | 适用场景 | 性能 |
|:---|:---|:---|
| `pandas.to_sql` + SQLAlchemy | 中小批量（<1万条），开发调试 | 中等，便捷 |
| `pymysql.executemany` | 大批量数据，生产环境 | 高，需手动处理 |
| `LOAD DATA INFILE` | 超大规模初始化（>100万条） | 最高，需文件中转 |

**事务与去重**：
```python
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker

engine = create_engine(
    "mysql+pymysql://user:pass@localhost:3306/quant_db",
    pool_size=10, max_overflow=20, pool_recycle=3600
)

def bulk_insert_upsert(df: pd.DataFrame, table_name: str):
    """批量插入，冲突时更新"""
    with engine.begin() as conn:
        # 使用INSERT ... ON DUPLICATE KEY UPDATE实现幂等写入
        df.to_sql(
            table_name, 
            conn, 
            if_exists='append',
            method='multi',  # 批量模式
            chunksize=1000,  # 分批大小
            index=False
        )
```

**Claude提示词（阶段2.3）：**

> "请为我实现Tushare数据接入层的Python代码，要求：1) 封装Tushare Pro API，设计`TushareDataClient`类，支持股票（`pro.daily`）、ETF（同股票）、基金（`pro.fund_nav`）、期货（`pro.fut_daily`）的日线数据获取，统一接口设计；2) 实现智能限流（令牌桶算法，默认200次/分钟）和指数退避重试（最多3次，间隔2s/4s/8s）；3) 数据获取采用'日期优先'策略：日线通过`trade_date`参数批量获取全市场数据（每年约220个交易日，效率高于按股票循环），分钟数据通过多线程并行加速；4) 包含完整的前复权/后复权处理，使用`pro.adj_factor`接口获取复权因子，提供`calculate_adjusted_price`函数；5) 批量写入MySQL，使用SQLAlchemy引擎配合`to_sql`的`method='multi'`和`chunksize`参数，实现事务管理和`INSERT ... ON DUPLICATE KEY UPDATE`去重；6) 完整的错误处理：网络超时重试、API限流等待、积分不足告警、数据异常记录；7) 提供可运行的示例代码，展示如何获取贵州茅台（600519.SH）2020-01-01至2024-12-31的日线数据，计算前复权价格，并入库到`daily_kline`表，包含数据质量校验（价格连续性、涨跌幅合理性）和性能监控（下载耗时、入库耗时、数据条数）。"

---
